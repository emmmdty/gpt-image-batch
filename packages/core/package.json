{
  "name": "@gpt-image-batch/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@gpt-image-batch/db": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "pino": "^10.1.0",
    "zod": "^4.1.13"
  }
}
